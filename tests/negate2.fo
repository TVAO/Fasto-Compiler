fun bool write_nl(bool b) =
  let res = write(b) in
  let tmp = write("\n") in
  res

fun bool main() =
  let x0 = write_nl(true) in
  let x1 = write_nl(false) in
  let x2 = write_nl(false) in
  let x3 = write_nl(true) in
  write_nl(x0 && x1 && x2)
